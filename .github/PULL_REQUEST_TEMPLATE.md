## What this changes

<!-- One or two sentences. -->

## The failure that caused it

<!-- Every rule here exists because a real session got past one. Which one? Paste it. -->

## Checklist

- [ ] `node tests/run.mjs` passes
- [ ] A case was added to `tests/run.mjs` for the thing being fixed
- [ ] If a hook pattern changed, a **false positive** case was added too
- [ ] `CHANGELOG.md` updated, and the version in `plugin.json` and `SKILL.md` still match
